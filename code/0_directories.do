
*All data inputs, temporary outputs and final outputs are here
global data "${master}data/"
global raw "${data}raw/" // only for raw data
global temp "${data}temp/" // for temporary outputs

*All do-files are stored here
global code "${master}code/"

*Graphs
global graph "${master}graphs/"


